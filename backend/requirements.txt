# Core API framework
fastapi>=0.111.0
uvicorn[standard]>=0.29.0

# ML stack (TensorFlow CPU is smaller and better for Render free tier)
tensorflow-cpu>=2.15.0
numpy>=1.26.0

# Image handling
Pillow>=10.0.0

# Data validation (comes with FastAPI but pinned for clarity)
pydantic>=2.0.0

# Multipart form data support (required for file upload)
python-multipart>=0.0.9

# Google Drive model downloader (used by download_models.py on Render)
gdown>=5.1.0
